*  指针
[] 数组   int *arr[10]
() 函数,改变优先级  int * func(int a)


void (*signal(int signm, void (*handler)(int sigum)))(int);

[]()优先级 >　＊
＊　在变量的左边
[]()　在变量的右边

rl法则：
只能用于复杂类型的声明,
一个复杂的数据类型,首先先找到唯一的一个不确定的变量,先往右边看,如果遇到")",则改变方向,遇到"("不改变方向.当把一个"()"分析完毕之后,把该"()"看成一个整体,然后再进项先右后左的分析.

int *p; p是一个指针
int *p[5];p是一个数组,数组有5个元素,每个元素都是int *型.
int (*p)[5]; p是一个指针,指针指向数组类型,该数组有5个元素,元素是int类型




1、复杂类型声明：
int (*func)(int * );
func是一个指针,指向一个函数,该函数有一个int *参数,返回值为int类型.

int (*func[2])(int * );
func是一个有两个元素的数组,元素是指针类型,该指针指向一个函数,函数有一个int *参数,返回值int类型.

int (*(*func)[2])(int *);
func是一个指针,该指针指向有两个元素的数组,数组元素的类型是指针类型,指向函数类型,该函数有一个int*参数,返回值为int类型的函数.

int (*(*func)(int *))[2];
func是一个指针,指向函数类型,有一个int *参数,返回值为指针,指向有两个元素的数组,元素类型是int类型

int*(*(*func)(int *))[2];
func是一个指针,指向函数类型,有一个int *参数,返回值为指针,指向有两个元素的数组,元素类型是int *类型


int (*func)(int *,  int (*)(int *));
func是一个指针,指向函数,第一个参数是int *,第二个参数是函数指针,指针指向函数,有一个int *参数返回值为int型
func指向的函数返回值为int类型

//void (*)(int)
// void (*)(int)
void (*signal(int signum, 
	void (*handler)(int sigum)))(int)
signal是一个函数,第一个参数int,第二个参数函数指针,指向有一个int参数,返回值为void函数
signal函数返回值为指针,指向有一个int参数,返回值为void的函数






int *(*(*func)[2])(int* (*ptr)[2], void (*b)(int (*c)[2]));
func是一个指针,指向有两个元素的数组,每个元素是指针类型,指向函数,第一个参数是指针,指向有两个元素的数组,元素类型是int *,第二个参数是指针类型,指向一个函数,该函数有参数,参数类型是指针,指向有两个元素的数组,元素类型是int类型,指向函数返回值为void类型.
外部的函数返回值为int *.










int (*)[2]
int (*(*ptr)(int (*)(int, int), int (*)[2]))[2]

int *(*func(int (*)[2], int (*(*ptr)(int (*)(int, int), int (*)[2]))[2]))[2];
func是一个函数,第一个参数指针,指向有两个元素的数组,元素类型是int类型,第二个参数是指针,指向函数,指向的函数,第一个参数是指针,指向函数,指向的函数有两个int类型的参数,返回值为int类型,第二个参数是指针,指向有两个元素的数组,元素的类型是int类型.ptr指向函数返回值为指针,指向有两个元素的数组,元素类型是int类型.func返回值是一个指针,指向有两个元素的数组,元素类型是int *.





int func[2](void);  
数组中不能放函数

int func(void)[2];
函数的返回值不能是数组







 
2、请为下列变量和函数编写声明：
1）p是指向函数的指针，并且此函数以字符型指针作为实际参数，函数返回的也是字符型指针。
2）a是含有4个元素的数组，且每个元素都是指向函数的指针，而这些函数都是没有实际参数且无返回值的。a的元素初始指向的函数名分别是foo1, foo2, foo3, foo4.
3) f是带有两个实际参数的函数：一个参数是指向结构的指针p, 且此结构标签为t; 另一个参数是长整数n. f返回指向函数的指针，且指向的函数没有实际参数也无返回值。
4) b是含有10个元素的数组，且每个元素都是指向函数的指针，而这些函数都有两个int型实际参数且返回标签为t的结构。
